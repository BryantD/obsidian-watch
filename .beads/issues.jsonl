{"id":"obsidian-watch-f95","title":"Execute configured command on file events with token substitution","description":"When a watched directory emits a create/modify/delete event, run the directory's configured command. Substitute {FILE} (basename), {PATH} (absolute path), {EVENT} (create|modify|delete), and {TIMESTAMP} (RFC 3339 UTC) before invocation.","design":"Substitution is pure and unit-testable: split out a fn substitute(template: \u0026str, ctx: \u0026EventContext) -\u003e String. Spawn with std::process::Command::new(\"sh\").arg(\"-c\").arg(rendered). Don't block the watcher thread waiting for the command to finish — spawn and let it run.","acceptance_criteria":"- Command runs via /bin/sh -c so shell features (pipes, redirection) work.\n- All four substitution tokens replace correctly, even when paths contain spaces.\n- Modify(Metadata) events are filtered out (per SPEC.md).\n- Unit tests cover token substitution given a synthetic Event.\n- Integration test: writing a file in a tempdir triggers the configured command and the command receives the right tokens.\n- Command spawn failures are logged to stderr but do not crash the watcher.","status":"open","priority":1,"issue_type":"task","owner":"durrell@innocence.com","created_at":"2026-05-15T10:02:47.776192-07:00","created_by":"BryantD","updated_at":"2026-05-15T10:02:47.776192-07:00","labels":["core","feature"]}
{"id":"obsidian-watch-yc4","title":"Debounce bursty filesystem events before invoking the command","description":"Obsidian (and most editors) save by writing to a temp file and renaming, producing 2-5 events per save. macOS FSEvents coalesces these; Linux inotify doesn't. Without debouncing, every save fires the command multiple times on Linux.","design":"Use notify::debouncer_full (already a sibling crate in the notify ecosystem) instead of rolling our own. It wraps recommended_watcher and exposes a coalesced event stream. Decide debounce window via config (per-directory or global) with a sensible default.","acceptance_criteria":"- A configurable debounce window (default ~250ms) coalesces events per (path, event-kind) tuple.\n- After the window with no new events for that key, fire one command invocation.\n- Test verifies that N events within the window produce 1 invocation; N+1th event after window produces 2.\n- Behavior verified on both Linux and macOS via CI.","status":"open","priority":2,"issue_type":"task","owner":"durrell@innocence.com","created_at":"2026-05-15T10:02:56.049113-07:00","created_by":"BryantD","updated_at":"2026-05-15T10:02:56.049113-07:00","labels":["feature"],"dependencies":[{"issue_id":"obsidian-watch-yc4","depends_on_id":"obsidian-watch-f95","type":"blocks","created_at":"2026-05-15T10:02:56.050195-07:00","created_by":"BryantD"}]}
